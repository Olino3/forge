# Python Code Review Tools
# Install with: pip install -r requirements.txt

# Modern Linting and Formatting
ruff>=0.1.0           # Fast Python linter and formatter (replaces flake8, pylint, black)
isort>=5.12.0         # Import sorter
basedpyright>=1.10.0  # Type checker (modern fork of pyright)

# Security Analysis
bandit>=1.7.5         # Security vulnerability scanner
safety>=2.3.0         # Dependency vulnerability checker

# Complexity and Quality
radon>=6.0.1          # Cyclomatic complexity analyzer (optional, ruff has some coverage)

# Performance Profiling
memory-profiler>=0.61.0  # Memory profiling
py-spy>=0.3.14        # Production-safe profiler (optional, requires compilation)

# Testing and Benchmarking
pytest>=7.4.0         # Testing framework
pytest-benchmark>=4.0.0  # Benchmarking plugin for pytest
locust>=2.15.1        # Load testing framework (optional)

# Additional Utilities
vulture>=2.9.1        # Dead code detector (optional)
