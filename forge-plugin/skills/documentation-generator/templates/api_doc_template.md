# [API_NAME] API

**Generated**: [YYYY-MM-DD HH:MM:SS]
**Skill**: documentation-generator v1.0.0

---

## Overview

[Brief description of the API, its purpose, and the domain it serves.]

**Base URL**: `[BASE_URL]`
**API Version**: [VERSION]
**Protocol**: [REST / GraphQL / gRPC]

---

## Authentication

[Description of authentication method.]

| Method       | Header / Parameter         | Example                              |
|--------------|----------------------------|--------------------------------------|
| [AUTH_TYPE]  | `[HEADER_NAME]`           | `[HEADER_NAME]: [EXAMPLE_VALUE]`     |

**Obtaining credentials**:
1. [Step to obtain API key or token]
2. [Additional steps if applicable]

**Token expiration**: [DURATION or N/A]

---

## Endpoints

### [RESOURCE_NAME]

#### [METHOD] `[PATH]`

[Description of what this endpoint does.]

**Parameters**:

| Parameter    | Type     | In      | Required | Default  | Description                |
|--------------|----------|---------|----------|----------|----------------------------|
| [PARAM_NAME] | [TYPE]  | [LOCATION] | [Yes/No] | [DEFAULT] | [DESCRIPTION]            |

**Request Body** (if applicable):

```json
{
  "[FIELD_NAME]": "[TYPE] — [DESCRIPTION]"
}
```

**Request Example**:

```bash
curl -X [METHOD] [BASE_URL][PATH] \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "[FIELD]": "[VALUE]"
  }'
```

**Response** `[STATUS_CODE] [STATUS_TEXT]`:

```json
{
  "[FIELD]": "[VALUE]"
}
```

**Error Responses**:

| Status | Code              | Description                          |
|--------|-------------------|--------------------------------------|
| [CODE] | [ERROR_CODE]      | [DESCRIPTION]                        |

---

[REPEAT ENDPOINT SECTION FOR EACH ENDPOINT]

---

## Common Error Codes

| Status | Code              | Description                                   |
|--------|-------------------|-----------------------------------------------|
| 400    | bad_request       | The request body or parameters are invalid    |
| 401    | unauthorized      | Missing or invalid authentication credentials |
| 403    | forbidden         | Insufficient permissions for this resource    |
| 404    | not_found         | The requested resource does not exist         |
| 409    | conflict          | The request conflicts with current state      |
| 422    | validation_error  | Request body failed validation                |
| 429    | rate_limited      | Too many requests — slow down                 |
| 500    | internal_error    | An unexpected server error occurred           |

---

## Rate Limiting

| Tier       | Limit                  | Window    |
|------------|------------------------|-----------|
| [TIER]     | [N] requests           | [WINDOW]  |

**Rate limit headers**:
- `X-RateLimit-Limit`: Maximum requests allowed in the window
- `X-RateLimit-Remaining`: Requests remaining in the current window
- `X-RateLimit-Reset`: Unix timestamp when the window resets

When rate limited, the API returns `429 Too Many Requests`. Implement exponential backoff in your client.

---

## Versioning

[Description of API versioning strategy.]

| Version | Status      | End-of-Life   |
|---------|-------------|---------------|
| [VER]   | [STATUS]    | [DATE or N/A] |

**Version selection**: [How clients specify the version — URL path, header, query param]

---

## Pagination

[Description of pagination approach.]

**Parameters**:

| Parameter | Type | Default | Description                     |
|-----------|------|---------|---------------------------------|
| page      | int  | 1       | Page number (1-based)           |
| per_page  | int  | 20      | Items per page (max [MAX])      |

**Response metadata**:

```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 150,
    "total_pages": 8
  }
}
```

---

## SDKs & Client Libraries

| Language   | Package              | Installation               |
|------------|----------------------|----------------------------|
| [LANG]     | [PACKAGE_NAME]       | `[INSTALL_COMMAND]`        |

---

## Changelog

| Date       | Version | Changes                              |
|------------|---------|--------------------------------------|
| [DATE]     | [VER]   | [DESCRIPTION]                        |

---

## Related Resources

- [Link to related documentation]
- [Link to OpenAPI/Swagger spec]
- [Link to Postman collection]

---

## Metadata

- **Generated by**: Claude Code documentation-generator skill v1.0.0
- **Generation date**: [YYYY-MM-DD HH:MM:SS]
- **Source files**: [LIST OF ANALYZED FILES]

---

<!--
Template Usage Instructions:

Replace all placeholders in [BRACKETS] with actual values:
- [API_NAME] - Name of the API (e.g., "User Management")
- [BASE_URL] - Base URL for all endpoints (e.g., "https://api.example.com/v1")
- [VERSION] - Current API version (e.g., "v1.0.0")
- [AUTH_TYPE] - Authentication method (e.g., "Bearer Token", "API Key")
- [RESOURCE_NAME] - Name of the resource group (e.g., "Users", "Projects")
- [METHOD] - HTTP method (GET, POST, PUT, PATCH, DELETE)
- [PATH] - Endpoint path (e.g., "/users/{id}")
- [PARAM_NAME] - Parameter name
- [TYPE] - Data type (string, integer, boolean, object, array)
- [LOCATION] - Parameter location (path, query, header, body)
- [STATUS_CODE] - HTTP status code (200, 201, 400, etc.)
- [ERROR_CODE] - Machine-readable error code (e.g., "validation_error")
- [TIER] - Rate limit tier (e.g., "Free", "Pro", "Enterprise")
- [WINDOW] - Rate limit time window (e.g., "per minute", "per hour")

Conditional Sections:
- Include "Authentication" only if the API requires auth
- Include "Rate Limiting" only if rate limits are enforced
- Include "Pagination" only for list endpoints
- Include "SDKs" only if official client libraries exist
- Omit empty sections entirely

Formatting:
- Use proper markdown syntax throughout
- Wrap code, paths, and parameter names in backticks
- Use tables for structured data (parameters, errors, etc.)
- Use fenced code blocks with language tags for examples
- Keep descriptions concise — one sentence per field
- Use imperative mood for instructions

Output Location:
- Save to the project's documentation directory or /claudedocs/
- Use descriptive filenames: api_{resource_name}.md
-->
